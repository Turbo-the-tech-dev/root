# 🏙️ IMPERIAL SCALING ORCHESTRATION
# "Directive 12: Scale Deployment"

version: '3.8'

services:
  electrical-api:
    build:
      context: ./Electrical-Core-API/
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: always

  # Future: Solid.js UI container
  # imperial-ui:
  #   build: ./Imperial-UI-v2/
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - electrical-api

"The Empire expands. No cap. ✌️"
